{
  "name": "entrevistas-meet-ia",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de Entrevistas Automatizadas con Inteligencia Artificial (leIA)",
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "install:all": "npm install && npm install --workspace=backend && npm --prefix frontend-angular install",
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm --prefix frontend-angular run start",
    "build": "npm run build --workspace=backend && npm --prefix frontend-angular run build",
    "start:backend": "npm run start --workspace=backend",
    "start:frontend": "npm --prefix frontend-angular run start",
    "seed": "npm run seed --workspace=backend"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
