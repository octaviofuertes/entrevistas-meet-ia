// Archivo de debug temporal del spike T06 — su contenido se consolidó en
// spike-live-api.ts. El sandbox de esta sesión no permitió eliminarlo
// (rm: Operation not permitted); se puede borrar manualmente sin riesgo:
//   rm backend/scripts/debug-live.ts
export {};
